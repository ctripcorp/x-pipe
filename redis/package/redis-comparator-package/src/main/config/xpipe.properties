# local override; production QConfig overlays these.
# CMS token / URL stay in QConfig only — do not put them here (spec §4.10 / T-PK.4).
console.address=
comparator.meta.refresh.interval.milli=30000
comparator.stream.buffer.bytes=2097152
comparator.compare.chunk.bytes=65536
comparator.mismatch.dump.bytes=128
